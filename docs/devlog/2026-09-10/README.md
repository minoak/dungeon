# 원더랜드 개발일지 초안 — 2026-09-10

커뮤니티 소개를 위한 검토용 자료. 외부 게시하지 않음.

현재 목표는 도구가 허용하는 정식 경로로 본문과 스크린샷을 게시한 뒤 게시 URL을 확인하는 것이다.
Chrome 연결은 확인됐지만, 글쓰기 주소 접근은 도구의 사이트 안전 정책으로 차단됐다.
차단 해소 전에는 게시를 재시도하지 않는다. 공식 지원 검토용 자료는 `support-request.md`에 있으며 아직 전송하지 않았다.

- `draft.md`: 처음 보는 독자를 위한 게임 소개와 실제 장면을 담은 익명 개발일지. 이미지 세 장을 본문 순서대로 배치했다.
- `draft-ojousama.md`: 같은 실제 장면을 오죠사마풍의 우아하고 장난스러운 말투로 소개하는 대안 원고.
- `images/01-armor.png`: t179, 수나 시점. 갑옷 전달 대사와 실제 전달 사건.
- `images/02-comfort.png`: t182, 유나 시점. 고블린 처치 후 친목 지문과 전달받은 갑옷.
- `images/03-relationship.png`: t246, 수나 시점. 동료 평가와 수정 전 30자 제한으로 잘린 지문.
- `sources.json`: 원본 파일 SHA-256, 사건 위치, 원본 결정·사건, 재생 URL.
- `capture.mjs`: 1440×1000 화면을 헤드리스 Edge에서 직접 캡처한다. 이미지 합성·대사 수정 없음.

원본은 `runs/stream-20260909-203709.jsonl`. 파일명과 run_meta의 시작 시각이 일치하는 실제 원정이다.
이 판의 캐릭터는 유나(전사), 수나(도적), 미나(궁수)다. 이후 다른 원정에서 바뀐 이름·직업 조합과 혼동하지 않는다.
원정은 t250 제한 시간에 끝났다. 이 글은 중간 장면을 발췌하며 클리어 기록으로 소개하지 않는다.

스크린샷은 원정 당시 그래픽을 보존한 사진이 아니라, 기존 결정 기록을 현재 게임 클라이언트로 재생한 화면이다.
직업별 자동 목표와 친목 30자 제한을 수정하기 전 원정이라는 사실을 본문에 명시했다.
친목의 자유 서술은 HP 회복·치료 등 물리 효과를 만들지 않으며, 쓰다듬기 전용 애니메이션도 현재 없다.

재캡처는 리포 루트에서 아래와 같이 실행한다. 기존 로컬 미리보기 서버를 사용하며 실 LLM을 호출하지 않는다.

```powershell
D:/node.exe docs/devlog/2026-09-10/capture.mjs
```

기본 주소는 `http://127.0.0.1:4197/game/`. 다른 서버는 `WL_GAME_URL`로 지정한다.
게시 시 `draft.md`의 이미지 위치에 PNG 세 장을 각각 업로드하면 된다. 커뮤니티 게시 규정과 로그인 가능 여부는 이 자료 작성 과정에서 확인하지 않았다.

공개 글과 이미지에는 작성자의 실명·개인 별칭·계정 식별 정보를 넣지 않는다. 게시할 자료는 본문과 스크린샷 세 장이다.
