# 변경 기록

## 2026-09-17 — 마을 체류 부검 도구 · 중앙 광장 논의 기록

- 파트너 질문 "캐릭터들이 하나같이 바로 던전으로 간다 — 강하게 목적을 준 거야?" → 0콜 부검 `tools/town_stay_audit.py`: 마을 시작 실판 14개의 마을 판단 352회 중 이유에 던전 준비 말 66%, 사람·구경만 2%(체류는 41~145틱). 명령 문장은 없고, 지침의 '제한 시간'+'내려가면 탈출' 두 줄(틱 상한이 마을 틱도 깎는다)과 던전 준비 일색인 마을의 말들이 원인.
- 논의 기록 [마을 부검과 중앙 광장 구상](town_plaza_2026-09-17.md): 중앙 광장(번화가 교차점 5×5 제안)·모이는 NPC·시간 제한은 첫 하강부터·여러 사용자 서비스의 3단계 경로(싱글 광장 → 다른 사용자의 저장 캐릭터가 주민으로 서는 비동기 멀티 → 공유 마을). **결정 아님 — 구현은 도구뿐**, 파트너 결정 대기 5건.

## 2026-09-17 — D81 론처 재편 1단계: 내 캐릭터 하나 · 동료 프리셋 · 마을 안내

- 론처의 '파티 → 던전' 두 장이 한 장이 됐다: **내 캐릭터** 편집 카드 하나 + **동료 칸 둘**(준비된 동료 또는 내가 저장한 다른 캐릭터를 고르기만 한다) + 두뇌·키 + **고급 설정 접이식**(기본 파티·원정 규칙·맵·마을 옵션·시드·가격표) + **[마을로 출발]**. 마을 시작이 기본이고 출발 버튼이 파티 저장과 시작을 이어서 한다. 저장한 캐릭터를 1번 칸에 세우면 카드 안에서 그 캐릭터의 원정 기록을 본다.
- 동료 프리셋 저장소 `entities/companion/<id>.json`(새 엔티티 종류): 시트 칸(능력치 없음 — 직업에서)·마을 주민일 때의 말·걸음·소개. 첫 셋 = 두란·카야·피른. 파일을 넣으면 론처 목록에 나온다. `/api/party` 칸에 `{"companion": id}`, `/api/presets.companions` additive.
- 마을 안내: 시작 마을의 첫 관측에 한 번 — 장소마다 정의의 특징 한 줄(새 문장 없음)과 동료가 선 구역(`DUNGEON_TOWN_GUIDE` 러너 기본 1, `run_meta.town_guide`). 정직 기록: 장소 줄은 매 틱 마을 관측과 겹치고, 새 정보는 동료의 위치뿐이다.
- 새 0콜 게이트 `verify_companion` 포함 69종. 브라우저 검증 둘을 새 흐름으로 갱신. 문구 ⚠️ 임시 149~176. 2단계(안 뽑힌 프리셋 = 마을 주민)는 다음.

## 2026-09-17 — D80 확장: 판단 가격표·로컬 API 키

- 로컬에서도 새 판·이어가기에 회사별 API 키 입력. 빈 칸은 기존 `.env` 키 사용, 화면 입력은 해당 자식 러너에만 전달하고 부모 환경·저장 옵션·파일에 남기지 않는다.
- 17종 공식 일반 텍스트 API 가격표와 글자 수 기반 예상 비용 계산기. 설정 1만 자 같은 가정, 현재 파티 설정 길이 적용, 추가 문맥·글자당 토큰·출력(사고 포함)·전체 판단 횟수 조절, 동일 조건 모델 비교. 실제 청구액·한 판 비용은 아니다.
- 할인 기한·긴 입력 단가 반영, 가격 미확인은 0원 대신 미확인 표시. 문구 ⚠️ 임시 143~148. 출처와 계산 범위는 [배선 기록](model_wiring_2026-09-17.md).

## 2026-09-17 — D80 확장: 사람이 모델 선택

- 새 판·이어가기의 모델 ID 입력 칸을 모델 선택 목록 + 직접 입력으로 확장. Gemini 9종·Claude 4종·OpenAI 4종, 총 17종. 기존 기본값과 저장된 사용자 지정 모델을 보존한다.
- Luna/Opus/Fable/Astra의 사고 설정을 요청에 반영. Fable/Astra 기본 출력 상한은 4,096, 명시한 환경변수는 우선한다. OpenAI 호환 주소에는 공식 OpenAI 선택 목록을 표시하지 않는다.
- 공식 모델 목록은 계정별 사용 권한이나 실호출 검증을 뜻하지 않는다. 문구 ⚠️ 임시 139~142. 검증·출처는 [모델 배선 기록](model_wiring_2026-09-17.md).

## 2026-09-17 — D80 다회사 모델 배선

- Gemini·Anthropic·OpenAI 호환 HTTP와 회사별 BYOK 로그인·열쇠 연결·새 원정·이어가기. 모델 ID 입력, 공통 슬롯 환경변수, 운영자 호환 URL 설정.
- 기본 모델의 공식 문서 확인, Gemini 신형 사고 설정 대응. 기존 호출 시그니처·전송 상한·실패 라벨·자동 폴백 opt-in 보존. 키는 선택 회사 러너 환경에만 전달.
- 계정 키 provider 태그 additive(이전 키=Gemini). 새 0콜 게이트 포함 68종. 모델 표·실측 한계: [배선 기록](model_wiring_2026-09-17.md). 문구 ⚠️ 임시 131~138.

> README 첫 화면을 다시 쓰면서(2026-09-13, 외부 이름 **봇픽던** 확정) 본문에 쌓여 있던 날짜별 변경 문단을 여기로 옮겼다.
> 옮긴 문단의 문장은 그대로다. 설계 결정의 정본은 [`design/HARNESS_DESIGN.md`](../design/HARNESS_DESIGN.md)(D1~D80), 커밋 단위 이력은 `git log`.

## 2026-09-16

- **D24·D76 개정 — 곁에 닿은 몸은 세우지 않는다.** 동료의 말(D24)이나 NPC 의 인사(D76)는 걷는 캐릭터를 세우지만, 이미 곁에 닿아 다음 틱에
  원래 행동(물품 받기·줍기·공격)을 실행할 참인 캐릭터는 세우지 않는다. 말은 편지함에 남아 그 행동이 끝난 다음 첫 결정에서 읽힌다.
  09-15 판 741197 부검: 접수원 곁에 닿은 수나를 미나의 말이 세워 물약 거래가 사라졌고("걷던 길이었다"도 거짓), 수나는 "방금 물약 챙겼어"라고
  믿은 채 0병으로 내려갔다 — 최근 마을 판 4개 중 3개에서 같은 자리. 파트너 "결함에 대해서는 동의". 게이트 `verify_hail ⑦`·`verify_guild ⑩`.
- **D77 계정 = 키의 지문 — 싱글 플레이 론칭의 첫 조각(소유).** 공개 서버에 가입·비밀번호 없이 자기 Gemini 키로 들어온다(`POST /api/login`).
  서버는 키의 생존을 구글에 한 번 묻고(폐기된 키는 문이 안 열린다 = 킬 스위치) 키의 지문(HMAC)으로 계정을 찾거나 만든다 — 그 뒤 캐릭터·판 기록은
  `accounts/<id>/` 에 남고 기기가 달라도 같은 키면 같은 계정이다. 키는 저장·기록되지 않는다(지문·별명만). 열쇠 여러 개(키 교체 = 들어와서 새 키
  연결), 마지막 열쇠 해제 불가, 로그인 IP 상한. 로그인 전 만든 캐릭터는 들어올 때 계정으로 옮겨진다. 패스키(①-b)는 다음 조각.
  파트너 "애초에 api키 자체를 아이디로 쓸 수는 없어?" · "로그인 했을 때 메뉴에서 패스키를 연결시키게 하면 그만 아냐?". 새 모듈 `accounts.py`, 게이트 `verify_account`(65번째).
- **D78 캠페인 — 저장한 캐릭터는 원정을 넘어 기억한다(②-a 기록 층).** 론처가 파티 칸의 출처(저장 캐릭터 id)를 파티에 동봉하고 러너가 `run_meta.party[].id` 로
  남기면, 캠페인 소비자(`campaign.py`)가 판 기록을 **마지막 줄까지** 읽어 캐릭터별 원정 이력·수첩 장·도감평·생사를 접는다(끊긴 판은 끊긴 자리까지, 진행 중인 판은
  '지금 3층 158틱'). 도감 원장의 키는 이름에서 저장 id 로(D64 가 미룬 것 — 같은 이름의 다른 캐릭터에 사건이 스미지 않는다). 계정 판(D77)은 원장을 계정 폴더에 두고
  저장 캐릭터만 남긴다. 즉석 슬롯·기본 파티는 1회용. `/api/characters` 에 기록 요약, `/api/characters/log?id=`. 파트너 "저장 시점은 마지막 기록에서 하는 게 어때?".
  몸까지 이어가기(스냅샷·요약·종료 전 알림)는 ②-b. 게이트 `verify_campaign`(66번째).
- **D79 이어가기 — 멈춘 판은 마지막 기록에서 이어간다(②-b).** 러너가 틱마다 루프 머리에서 판의 몸 전체(세계·보존 층·파티·편지함·장부·발급기·결산)를 `state/snapshot.pkl`
  에 얼린다(~100KB·1ms). 론처 중지·서버 재시작·크래시 어느 경우든 마지막 틱과 같은 몸이 남고, 타이틀의 **이어가기**가 그 몸을 되살려 **같은 기록 파일에** 이어 쓴다(`resume` 줄,
  틱 번호 연속, 캠페인은 한 항목). 중지 버튼은 먼저 알린다("마지막 기록에서 이어갈 수 있다") — **수첩 쓰고 멈춤**이면 캐릭터들이 지금까지의 이야기와 하려던 일을 수첩 한 장씩
  적고(`stopped` 줄, 캐릭터당 1콜) 이어갈 때 '기억해두기로 한 것'에 들고 간다 + 첫 관측에 "원정을 이어간다" 한 줄. 되살리기 실패(엔진·설정이 바뀜)는 옛 기록을 `runs/` 로
  대피시키고 새 판을 열되 수첩 장은 유지(`run_meta.resume_failed`). 파트너 "저장 시점은 마지막 기록에서" · "던전에서 이어가게 하자 … 요약해서 들고 있게 … 종료 전에는 미리 알림".
  새 모듈 `snapshot.py`, 게이트 `verify_resume`(67번째 — 이어간 판의 틱 기록 = 끊기지 않은 판과 바이트 동일).

## 2026-09-15

- **D76 NPC 인사에 걸음을 멈춘다** — NPC 가 먼저 말을 걸면(D71) 걷던·기다리던 캐릭터가 그 자리에 서서 그 틱에 다음 행동을 고른다
  (`DUNGEON_NPC_HAIL_STOP` 기본 1, `tick.npc_hails[].stopped`). 파트너 "npc가 캐릭터에게 말을 걸릴 때도 멈추게 하자" — 09-14 판에서
  자동보행 중 받은 인사 둘을 그냥 지나친 것의 답. 직전 결과 문장 "성직자의 말에 걸음을 멈췄다 — 걷던 길이었다". 게이트 `verify_guild ⑩`.
- **D75 장소·사람의 이야기** — 건물 4·마을 NPC 6·구역 6·마을 1의 정의에 `story{trait, history}` 부품. 특징 한 줄(trait)은 멀리서도 이름 옆에
  ("성직자 f1 (기도 · 신의 요청) — W, 거리 27 — 기도를 받고 축복의 물약을 준다"), 이야기(history)는 곁 2칸에서만 한 문단, 구역 특징은
  "지금 있는 곳" 줄에, 마을 전체 이야기는 마을에 들어서며 한 번. 파트너 "각 장소나 오브젝트에 … 역사나 이야기 그리고 장소의 특징을
  정해두는게 좋아보이는데". 도감 지식(knowledge)과 다른 층(해금 없음). 문장 17벌은 전부 임시(파트너 교정 대기 — `docs/wording_review_2026-09-15.md`).
  게이트 `verify_guild ⑭`.
- **D74 기도의 답 = 축복의 물약** — 신전 성직자에게 기도(`use`)하면 축복의 물약을 한 병 받는다(방문당 한 번). 마시면(`use self item=i4`,
  메뉴형 `drink item=boon`) 공격에 쓰는 능력치가 1 오른다 — 근접은 힘, 원거리는 민첩(공격 판정과 같은 갈림). 이 판 안에서 영구(층 이월).
  곁의 동료에게 건넬 수 있다(`give`). 관측에 병 수와 "마시면 힘 +1"(사실), 곁의 동료는 마시는 장면을 목격. 파트너 "기도효과는 스테이터스
  증가+1의 물약을 하나 주는걸로 하자". 게이트 `verify_guild ⑬`. 성직자 대사는 임시(파트너 문장 대기).
- **계단 줄 정합** — 마을 계단 줄의 "카야 보임, 아직 멀다"가 파티 명단의 "시야 밖"과 모순이던 것(구역 지각 D70 이전 판정) 수선 — 동료 보임은
  단일 판정처(`_ally_seen`).

## 2026-09-14

- **D69 길드 척추** — 마을 게시판의 의뢰를 맡을 수 있다(`use q<n>`, 파티의 의뢰). 완료는 엔진이 센다(처치·층 도달·획득). 워프게이트로
  돌아와도 판이 끝나지 않고 마을에서 이어지며, **길드 접수원에게 보고하면 원정이 끝난다**(`returned`). 관측에 건물·NPC 의 역할 한 줄
  ("모험가 길드 (의뢰 게시판 · 원정 물품 · 귀환 보고)"). 결산·관전 로그·결말에 의뢰 줄. 게이트 `verify_guild`(64번째).
- **흩어진 출발**(D69) — 마을 시작 때 셋이 광장에 붙어 서는 대신 각자 건물(길드·주점·신전) 문턱 곁에서 시작한다(론처 기본 켬).
- **마을 행인**(D73) — 떠돌이 모험자·견습 모험자(파티를 못 구한)·노점 상인이 번화가와 샛길을 걸어 다닌다. 같은 구역에 있어야 보이고,
  말을 걸면 답하고(D69), 가까이 오면 먼저 말을 건다(D71). 사고파는 건 아직 없다. 도트는 파트너 에셋 대기(그동안 기본 타일).
- **말의 결과 되먹임**(D72) — 동료를 지목해 말하면 누가 들었는지가 다음 결정에 돌아온다("들은 사람 없음: 시야 안에 아무도 없어 혼잣말이
  됐다"). 혼자 있으면서 동료 이름을 부르는 대사가 왜 헛도는지를 규칙이 아니라 결과로 알려 준다.
- **NPC가 먼저 말을 건다**(D71) — 캐릭터가 접수원·주점 주인·성직자와 같은 구역, 여섯 칸 안에 오면 NPC가 상황에 맞는 한마디를 먼저 건넨다
  (물약이 없으면 "물약도 없이 가시게요?", 안 맡은 의뢰가 있으면 게시판 안내, 돌아온 파티엔 "보고하시겠어요?", 주점 주인은 1층 실측 소문,
  성직자는 신의 요청이 있을 때). 캐릭터당 NPC당 한 번, 잡담이라 아무도 멈추지 않고 LLM 호출도 없다. 다가갈지는 캐릭터 몫.
- **마을 사람 지각은 구역 단위**(D70) — 마을에서 길과 건물은 다 알지만, 동료가 보이고 말이 들리고 사건을 목격하는 건 같은 구역(번화가·
  샛길·신전 지구·길드 지구·주점 지구·던전 입구 구역) 안이거나 곁일 때만. 흩어져 출발하면 정말로 흩어진다. `DUNGEON_TOWN_HEAR=all` 로 옛 전체 시야.
- **마을 NPC 두뇌**(D69) — 캐릭터가 접수원·주점 주인·성직자에게 말을 걸면 NPC 의 한마디를 LLM 이 쓴다(말 걸 때만 1콜, NPC 는 먼저
  말하지 않음, 물품·보고 판정은 엔진, 실패하면 정해진 대사). 주점 주인은 지하 1층의 실제 배치를 숫자로 안다(소문 재료). 관전 화면에
  NPC 말풍선. 프롬프트 `prompts/npc_prompt.md`(⚠️문장 임시).

## 2026-09-13

- **D68 공개(심사용) 서버 `server.py`** — `launcher.py` 를 심사위원(쿠키 세션)별로 감싼다: 세션별 state/·runs/·파티 파일, 정적 허용 목록(.env·소스·남의 세션은 404), BYOK(판 시작의 키가 그 판 러너의 환경변수로만 들어가고 로그·디스크에 안 남음), 상한(동시 3판·세션 1판·IP 시간당 12). 론처 화면에 키 칸(공개 서버에서만). `verify_public` 62번째 게이트. 배포는 `scripts/vm/`(GCP 서울 VM + Caddy HTTPS).
- **첨부 판 7개** — 정적 배포(Pages)의 첨부 판을 09-09 판 하나에서 최신 판 위주 7개로: 마을→4층(2, 기본 판은 도감·수첩 포함)·보스방 앞→보스 처치→귀환·마을→5층(2, 하나는 사망 1)·던전 600턴, 그리고 스모크 기준 판(257573). `game/static-runs.json` 항목에 `{path, note}` 꼴(note = 드롭다운 라벨 앞머리)을 허용했다.
- **외부 이름 봇픽던**(고유명사로만 쓴다 — '봇 픽셀 던전' 풀어쓰기는 공식 문서에 쓰지 않는다: 기존 게임 Pixel Dungeon 과의 혼동·포크 오인 회피) — README·론처·관전 화면·옛 뷰어·리플레이·보고서 제목과 `game/package.json` 이름(`botpikdun-game`)을 바꿨다. 코드·파일명의 `wonderland`(원더랜드)는 내부 코드명으로 남긴다. README 를 첫 화면 용도로 다시 썼다(스크린샷 5장 새로 캡처: 보스전·마을 출발·게이트 귀환·도감 창·론처).
- **리포 정리 1단계** — 프롬프트 → `prompts/`(옛 판본 `prompts/legacy/`), 분석·부검·리플레이 도구 → `tools/`, 셸 스크립트 → `scripts/`, 7월 A/B 실험 산출(`ab_runs/`·`reports/`)과 죽은 스크립트·`risu/` 삭제(이력에 있음), `state/`는 `.gitkeep`만 추적, `LICENSE`(MIT) 추가. 지도는 [`repo-map.md`](repo-map.md).
- **D65 개정** — 지하 5층에 들어설 때 "이 층에 보스가 있고 워프게이트는 봉인돼 있다"는 세계의 사실을 한 번 들려준다.
- **D61 개정** — 관전자의 신탁은 어느 층에서나 전원에게 들린다("신의 요청"이며 명령이 아니다).
- **D67** — 론처 '보스방 앞에서 시작' 프리셋(최심층·보스룸 앞, 마을 없음).
- **D66** — 작정(계획) 기본 끔, 대기 상한 15→5틱과 곁 도착·시야 이탈 깨움, 계단 줄에 "살아 있는 일행 전원이 곁에 모여야 함께 쓴다" 규칙을 미리 적음.
- **D65** — 최심층 보스(고블린 대장)와 봉인 워프게이트(보스 처치로 해제), 마을 귀환 = 판 종료(`returned`).
- **D64** — 캐릭터 영속(도감 이월)은 서빙 단계의 기능. 지금은 판마다 별개, 론처 '도감 이월' 기본 끔, `bestiary.json` 미추적.
- **D63** — 관전 화면의 도감·수첩 창(파티가 알게 된 몬스터와 각자의 도감평, 층마다 쓴 수첩 한 장, 재생 위치까지).
- **D62** — 프롬프트가 모델 안전 필터(Gemini PROHIBITED_CONTENT)에 걸리면 몸짓 서술 줄만 접고 같은 두뇌에 다시 묻는다. 접은 결정은 `brain_degraded` 표식이 남는다.

## 2026-09-12

- 성격 키워드는 문장으로 바뀌지 않고 그대로 시트에 들어가며(D31 개정), 자유 서술과 합쳐 2,500자까지 프롬프트에 전달한다.
- 관전 정적 빌드(`npm run build:static` → `dist-static/`, GitHub Pages 워크플로)와 첨부 판 목록(`game/static-runs.json`).
- 동료 상태에 '대기중'·'(이동 중 → 출구)' 같은 지금 하는 행동을 표시한다(D25·D27 개정).
- 장비 개체화(D57): 같은 이름의 장비도 번호로 구분하고 '착용해 봄' 태그를 남긴다.
- 판 결산(D58, `run_summary.py`)과 수첩(D59, 층을 떠날 때 캐릭터 한 장).

## 2026-09-11 — 스킬 원정 기본 채택 · 엔티티 저장소 · 마을 v1

**스킬 원정 (2026-09-11 기본 채택)**: 프리셋 스킬 4개, 선택 TRPG 판정, 3층 랜덤 스킬 획득을 추가했다.
`wonderland.bat`의 **L: LAUNCHER → 새 원정**으로 시작하면 기본 적용된다. 이전 A 진입도 같은 원정으로 연결된다.
규칙·시작 스킬 미리보기 → 5층 원정(최대 600틱) → 실시간 관전으로 이어진다. 이전 규칙은 비교용 옵션으로 남긴다.
캐릭터 성격은 2,000자, 배경은 4,000자까지 입력·저장할 수 있다. 성격 키워드는 문장으로 바뀌지 않고 그대로 시트에 들어가며(2026-09-12), 자유 서술과 합쳐 2,500자까지 프롬프트에 전달한다.
`python tools/run_skill_alpha.py --arm full --backend dummy`로 별도 폴더에서 기술 스모크를 돌릴 수 있다.
기본 채택 후 공식 게이트 53종과 브라우저 검사 19개를 통과했다. 여러 판의 밸런스 평가는 별도로 이어간다.
[실행·확정 규칙·검증 범위](alpha-skills.md)를 참고한다.

**엔티티 저장소(D50, 2026-09-11)**: 몬스터·함정·오브젝트·마을 NPC 의 정의(수치·이름·지식 본문·대사·선물)는 `entities/<kind>/<id>.json`
한 곳에 있고 엔진이 거기서 읽는다(옛 `lore.json`·코드 상수·`town.json` 대사를 이관 — 같은 시드의 결과는 해시까지 그대로).
새 정의를 넣는 법과 검증(`verify_entities.py`)은 [entities.md](entities.md).

D48: `follow` 제거 — 사람에게 `goto` 는 그가 움직이면 뒤쫓고 곁에서 멈추면 재판단. 마을 v1 채택(레이아웃 `layout.json` → 격자 컴파일).

## 2026-09-10 — 조합형 행동 방식 기본

2026-09-10: **조합형을 기본 행동 방식으로 사용한다.** 새 원정에서는 별도 방식 선택 없이 조합형 지침이 적용된다.
`self`, 관측별 길 ID, 소지품 ID, `use` 통합과 자동 접근을 지원한다. 알려진 대상과 COMMON 9개 행동(2026-09-11 D48로 `follow` 제거 — 사람에게 `goto` 는 그가 움직이면 뒤쫓고 곁에서 멈추면 재판단)의 조합을 세계가 판정하며, 성공·실패·변화 없음을 구분해 기록한다. 받은 상호작용에 선택적으로 남기는 like/dislike와 관전용 캐릭터·층·원정 집계도 지원한다([반응 기록 안내](reactions.md)).
실행 지침은 [prompts/adventurer_prompt.md](../prompts/adventurer_prompt.md), 이전 메뉴형·자유서술형은 [날짜별 백업](../prompts/legacy/2026-09-10/README.md)에 보존했다([적용 범위](compose-probe.md)).

## 2026-09-09 — 관전 클라이언트 · SD 도트 외형 · 캐릭터 프리셋

게임 클라이언트 `game/`(Phaser 3 + TypeScript + Vite) 1차 완주 — 초점 캐릭터 카메라, 말풍선, 이야기 탭. 옛 HTML 뷰어(`viewer/`)는 그대로 열린다.

2026-09-09: **서브컬처 SD 도트 외형 3종**(검사·궁수·도적, 4방향 정지/걷기)을 웹 론처·관전 뷰어에 추가했다.
내장 이미지 생성으로 만든 원본·프롬프트·변환 도구·검토 화면은 [`art/sprites-v4/`](../art/sprites-v4/README.md),
투명 PNG 런타임본은 `viewer/assets/sprites/sd/`에 있다. 론처에서 완성 외형 또는 기존 파츠 조합을 선택하며,
완성 외형 id는 `look.sprite`에 저장된다. 새 커스텀 파티의 초기 미리보기는 SD 외형을 쓰고, 기존 판의 외형은 유지한다.
기본 머리를 포함해 **검사 헤어 5종, 궁수·도적 각 3종**을 선택할 수 있고, 선택은 `look.hairstyle`에 저장되어 관전·리플레이에 이어진다.
검사에는 얼굴·의상을 유지하며 여성적인 헤어 인상을 비교할 수 있는 긴 머리와 양갈래를 추가했다.
HTML 뷰어는 교체 예정인 임시 뷰어다. 에셋은 PNG와 `atlas.json`으로 관리하며, 다음 뷰어의 연동 규격은 `art/sprites-v4/README.md`에 기록한다.
걷기 발 교차와 방향별 소품 일관성은 더 다듬을 여지가 있는 1차 적용본이다.

**캐릭터 프리셋**: 커스텀 파티의 각 카드에서 캐릭터를 꾸민 뒤 **새로 저장**한다. 저장한 캐릭터를
목록에서 골라 **불러오기**하면 어느 파티 칸에도 재사용할 수 있다. **덮어쓰기**는 선택한 저장본을 수정하고,
**새로 저장**은 같은 캐릭터의 다른 버전을 만든다. 이름·성별·직업·성격 키워드와 자유 서술·배경·외형·헤어를
`character_presets.json`에 보관하므로 브라우저나 론처를 다시 열어도 남는다. 이 개인 파일은 Git에서 제외되므로
별도로 백업한다. 목표는 자동으로 채우지 않으며, 관계 기억은 프리셋에 포함하지 않는다.
임시 HTML 뷰어 교체 후에도 같은 저장 파일과 API를 사용할 수 있다([저장 규격·검증](character-presets.md)).

커스텀 캐릭터의 직업은 능력치만 정하며, 직업별 행동 목표를 자동 부여하지 않는다(2026-09-09 사용자 정정).
목표를 입력하지 않으면 시트·프롬프트·새 판 기록에 `goal`을 추가하지 않는다. 직접 작성한 캐릭터 시트의 선택 필드 `goal`은 지원한다.
이전 버전에서 이미 저장한 커스텀 파티에는 자동 목표가 남아 있을 수 있으므로 해당 `goal`을 지우거나 새로 저장한다.
수정 전부터 켜 둔 론처는 진행 중인 판을 마친 뒤 다시 실행해야 변경된 조립 코드가 적용된다.

## 2026-09-06 — 캐릭터 외형 = 파츠 스프라이트(D37)

캐릭터 파츠 스프라이트는 파트너 자작 — 원본 시안·변환 도구는 `art/sprites-v1/`, 뷰어·론처가 읽는 런타임본은
`viewer/assets/sprites/sprites.json`(16×16 팔레트 인덱스 행렬, 캔버스에서 합성 — PNG 없음), 색 스와치는 `looks.json`.
외형은 시트 `look` 필드 또는 러너의 시드 랜덤으로 정해져 판 기록(run_meta)에 남는다 — 엔진·프롬프트는 읽지 않는다.

## 그 전

7~8월의 결정(GM 강등·시야-온리·리모컨 메뉴·A/B 실험 D19 등)은 [`design/HARNESS_DESIGN.md`](../design/HARNESS_DESIGN.md)의 D1~D30 과
[D19 실험 요약](D19_experiment_summary.md)에 있다.
