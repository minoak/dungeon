# 실험 사전등록 — "보고 이동"(D19) 미로 판정 (2026-07-11)

**발단(창립 관찰, 민옥)**: "이동이 수동적이다 — 허허벌판에서 어디로 갈지 근거가 없다."
이 실험은 그 관찰을 숫자와 육안으로 판정한다. 지표·판정 규칙은 **암 B 실행 전에 이 문서로
고정**한다(사전등록 — 리모컨·페르소나·ascii 프로브 선례).

## 장면

`scenarios/미로탈출.json` — 손그림 지도(47×21, 바닥 350칸 전부 연결 검증):
시작 방 → 본 복도(T자 갈림길 2, 막다른 길 3) → 작은 방(문 3) → **시야(5)보다 큰 공동
15×17**(중심 (38,11)에서 벽 불가시 검증) → 출구(공동 북동).
1인(두란), 몹 0·함정 0 — 순수 이동만 측정. 150틱, seed 7, 리모컨 기본(menu=1).

- 공동 사각형 = (31..45, 3..19). 계측기 `maze_metrics.py` 와 지도가 한 몸(지도 수정 시 갱신).
- 결정론 참조치(dummy 두뇌): escaped 90틱 / 걸음 78 / 재방문율 0.103 / 공동 15틱 / 결정 12회.
- 알려진 조건 왜곡: `from_ascii` 는 전 바닥을 단일 방 r0 으로 등재(구역 어휘 뭉개짐) —
  **양 암 동일 조건**. 암 B 구현에는 기하 기반 구역 분류(방/복도)가 선행되어야 한다.

## 암

- **암 A(현행)**: 오늘 HEAD 의 wire/어휘 그대로. ways=방향·거리·발자국뿐, 구조 침묵.
- **암 B(D19 패키지)**: 공간 어휘(방·문·복도·갈림길·막다른 곳 + 장소 딱지 3단) + wire 새 구성
  (장소: / 보이는 것: 라벨식) + 탐색 종점 개정("새 명사가 나타나면 멈춤").
  패키지로 비교한다 — 승패 요인(어휘 vs 구성)은 가르지 않는다(제품 판정이지 논문 아님).

## 규약

- 각 암 **3판**, 지표는 **중앙값**. 실LLM 단독 실행(다른 실판과 동시 금지 — 경합 오염 전례).
- 판 보존: `runs/maze-<arm>-r<n>-<날짜>.jsonl` (state_* 는 gitignore — 실데이터는 runs/ 로).

## 지표

주지표(판정용):
1. **탈출 틱** (escaped 기준 — timeout 이면 그 판은 최하위로 취급)
2. **재방문율** — 이미 밟은 칸으로의 걸음 / 전체 걸음 (헤맴의 숫자)
3. **공동 소요 틱** — 공동 진입 → 탈출 (허허벌판 문제의 직접 측정)

보조지표(판정 미사용, 관찰 카드):
- reason 의 공간 근거 인용(손세기 — "막다른 길이라", "안 가 본 문으로" 류)
- 폴백률·결정 수 (타임아웃 소음 전례 — 판정에 쓰지 않는다)

## 판정 (사전등록)

- **암 B 가 주지표 3개 중앙값에서 암 A 에 지지만 않으면(무승부 포함) 채택.**
- 주지표 2개 이상에서 뚜렷이 악화되면 기각 — 어휘 재설계로 회귀.
- **최종 거부권 = 민옥 육안**: 뷰어/로그로 봤을 때 "이동이 능동적으로 보이는가".
  숫자가 이겨도 육안이 지면 재론한다(창립 관찰이 육안이었으므로).

## 기록

- 2026-07-11: 지도·계측기·본 문서 작성, 더미 참조치 실측. 암 A 3판 실행 개시.
- 암 A 결과: (기입 대기)
- 암 B 결과: (D19 구현 후)
