── ① 기하 분류(from_ascii 다방)
  OK   ① 손그림 맵에서 방 2 + 통로 1 (출생기록은 단일 방이었다)
  OK   ① 전 바닥 칸이 정확히 한 구역에 배정
  OK   ① 통로=폭1 외길(2×2 블록 밖), 방=블록 소속
  OK   ④ 두 방 = 두 주소(단일 r0 뭉개짐 치료)
── ② 사전등록 미로(EXP_D19_MAZE.md)
  OK   ② 방 3 · 문 5 · 갈림길 2 · 막다른 곳 3
  OK   ② 공동 = 15×17 (시야 5 보다 큰 방)
  OK   ② 계단은 공동 안(수색이 정복할 미지)
  OK   ② 작은 방 = 문 3(사전등록 문구 그대로)
── ③ 생성 맵 스윕(격자·문 타일 불변식)
  OK   ③ [12시드] 전 바닥 배정(빠짐·중복 없음, 문 타일은 구역 밖)
  OK   ③ [12시드] 문 타일 스탬프 실재(96개) + 측칸·트임 정합
  OK   ③ [12시드] scan=0 격자에 문 타일 전무(스위치 순수성)
  OK   ③ [12시드] 스캔 결정론(같은 격자=같은 구역·문·문 타일)
── ④ 장부 주소 = 기하 구역 명의
  OK   ④ 보이는 상자 장부 등재 주소='방 r0'(기하)
  OK   ④ 방문 구역 = 기하 id 열쇠
── ⑤ 시야 제한 조회 — 네 눈이 본 만큼이 네가 아는 만큼
  OK   ⑤ 다 못 본 방 = 크기·상대위치 없음 + full=false + 미답 방위(todo)
  OK   ⑤ 문 어휘 = 눈에 든 것만(doors_seen 명의 — 미목격 문 실증은 ⑨ 한복판 봇)
  OK   ⑤ 계단=내용물: 안 보이면 obs.zone·옵션·brains 타겟 어디에도 없다(07-12 정정 유지)
  OK   ⑤ 확인 딱지 따라 수색 → 계단이 시야에 들면 sighted 정지(발견의 순간)
  OK   ⑤ 이제 보이는 계단 = sights·타겟에 등장(보일 때만 규칙 그대로)
  OK   ⑤ 발견 후 핑 → 계단 앞(at_exit)
── ⑥ 문 핑(지나 들어서기)
  OK   ⑥ 아는 문이 이동 옵션에 있다(1:1 — 문 어휘의 리모컨 편입)
  OK   ⑥ 문 id 는 _valid_targets(goto)에, interact 엔 없음
  OK   ⑥ 문 통과 → 문턱이 아니라 '다음 공간'(통로)에 들어선 채 재결정
  OK   ⑥ 문 너머 처음 방 = 들어선 채 완료(entered 정지 폐지 — 볼 게 있으면 sighted 가 알린다)
── ⑦ 정지 신호 = 새 오브젝트가 시야에 들 때(벽 제외)
  OK   ⑦ 스폰에서 보이던 문 = seen_keys 씨딩(개시 때 보인 건 '새것' 아님)
  OK   ⑦ 걷다 새 문이 눈에 들면 sighted 정지 + 남은 작정 파기
  OK   ⑦ 다시 걷다 계단이 눈에 들면 sighted 정지(멈춰 묻는다)
  OK   ⑦ 처음 방 진입은 정지 사유 아님 — 곧장 계단까지(entered 폐지)
  OK   ⑦ 다 본 길 되짚기 = 정지 없이 끝까지 관통(볼 게 없으면 멈출 이유도 없다)
── ⑧ 탐색 종점
  OK   ⑧ 방에서 탐색 = 문을 지나 다음 공간까지(시야 가장자리 정지 폐기)
  OK   ⑧ 빈 복도 = 막다른 곳까지 한 order 로 끝까지(중간 재결정 없음)
  OK   ⑧ 개시 때 이미 보이던 계단은 '새것'이 아니다(두 leg 모두 계단 정지 없음)
── ⑨ 시야-온리·출처 딱지
  OK   ⑨ 구조 obs 에 좌표 무노출(방위·거리·딱지뿐)
  OK   ⑨ 문 항목 스키마 고정({id,bearing,dist,seen,been})
  OK   ⑨ 미답 공간 한복판 = 문 어휘 없음·크기 모름(전지성 제거 실증)
  OK   ⑨ 숨은 함정·매복몹 = obs·wire 침묵(발각 전엔 없다)
  OK   ⑨ 발각된 함정 = sights.traps + wire (발각됨) 딱지
── ⑩ wire 전수성 — 다시드·전위치 스윕
  OK   ⑩ [347뷰] 8방위 슬롯 전부 발화
  OK   ⑩ 실린 문이 제 방위 슬롯에 등장(그림≡문장)
  OK   ⑩ 보이는 몹 전부 문장에 등장
  OK   ⑩ '벽' 발화는 정말 빈 방향뿐
  OK   ⑩ 문턱(문 위) = zone kind '문턱' + wire 무사고
── ⑪ 스위치
  OK   ⑪ scan 꺼짐 → 구조 어휘 전무(zone·traps·문 옵션 없음)
  OK   ⑪ scan 꺼짐 → wire 는 구판 구성(지금 보이는 것)
  OK   ⑪ scan 켬 → wire 는 트리(장소 절)
── ⑫ scan-on 풀게임(더미) 종결·결정론
  OK   ⑫ [30시드] scan-on 풀게임 항상 종료(목격 정지가 교착을 만들지 않는다)
  OK   ⑫ [8시드] 결정론 — 같은 시드 = 같은 판(구역·문·목격 장부 서명 포함)
── ⑬ 문 광학 — 벽처럼 막고 바닥처럼 지나간다
  OK   ⑬ 문 너머 불가시(문 자체는 보인다) · 문 위=양쪽 가시
  OK   ⑬ 문 통행 가능(BFS·봇·몹 공통 지형)
  OK   ⑬ 손그림 문(+)도 광학 차단 — 계단이 문 뒤라 안 보인다
  OK   ⑬ 문턱에 올라서는 순간 다음 공간이 열린다 — 그 자리서 sighted(계단 발견)
  OK   ⑬ 발견 후 핑 → 계단 앞(at_exit)
── ⑭ 탐색 폴백 — 걷는 곳은 캐릭터가 아는 곳뿐
  OK   ⑭ 계단 미목격·새 길 없음 → 기억 속 안 가 본 문(d1) 건너편으로(door 병기, to_exit 없음)
  OK   ⑭ 계단을 본 적 있으면 기억의 계단행(to_exit+remembered)
  OK   ⑭ 문은 다 가 봤고 계단 미목격 → 기억 속 안 본 가장자리로(frontier)
  OK   ⑭ 전부 소진 → no_path+exhausted·order 파기(안 본 계단으로 안 걷는다)
  OK   ⑭ 소진 시 '탐색' 어휘 없음 + obs.exhausted
  OK   ⑭ 렌더 '새 길이 없다'(관찰 사실만, 물음표 0)
  OK   ⑭ 자기 관측 문장(exhausted)
  OK   ⑭ 갈 곳(기억 속 문)이 있으면 '탐색' 어휘 있음
  OK   ⑭ scan 없는 판(평생 시야 장부 없음)은 구 폴백 유지(to_exit, remembered 없음)
  OK   ⑭ 결정론
============================================
RESULT: ALL PASS — 스캐너·보고 이동(D19 정정판) 계약 건전
