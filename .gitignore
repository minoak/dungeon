__pycache__/
*.pyc
state_*/
# 스모크 실행의 콘솔 덤프 — state_*/ 규칙에서 새던 것 (2026-08-24 정리)
state_*.out

# 비밀값(2026-07-25 두뇌 백엔드 어댑터) — 이 레포는 state/ 와 runs/*.jsonl 을 실제로
# 커밋한다. 키가 한 번 히스토리에 박히면 git rm 으로 못 지운다(rewrite + 키 폐기가
# 유일한 복구). 그래서 .env 를 만들기 *전에* 이 줄들이 먼저 있어야 한다.
.env
.env.*
!.env.example
*.key

# Claude 세션 기억 스냅샷(로컬 전용) — 공개 리포에 실리면 안 된다.
# 2026-08-10 filter-repo 로 히스토리에서도 제거했음. 로컬 폴더는 보관.
claude-memory/

# 론처(D31, 2026-09-05)가 만드는 사용자 파일·러너 콘솔 덤프 — 개인 저작물이라 커밋 안 함
party_custom.json
character_presets.json
state/runner.out
state/brain_pause.json
state/brain_retry.json

# 스프라이트 묶음(D37, 2026-09-06) — 재생성물(합성 PNG 600장+sprites.json)은 커밋 안 함.
# 런타임 유일본은 viewer/assets/sprites/sprites.json(사본). 원본 시안·도구·README 만 art/ 에 둔다.
art/sprites-v1/assets/
*.zip
# 차단 프롬프트 원문(시트 포함) — 진단용, 개인 파일(2026-09-13)
state/brain_block.log
# 신탁 소켓 세션 입력(D61) — 판마다 다른 값(2026-09-13)
state/oracle.json
# 도감 원장 — 캐릭터 영속은 서빙부터(D64, 2026-09-13). 이월을 켠 판만 로컬에 쌓고 저장소엔 안 올린다
bestiary.json
# 2026-09-13 리포 정리(1단계): 실행 흔적·끝난 실험 산출은 추적하지 않는다(state/ 는 폴더만 .gitkeep)
state/*
!state/.gitkeep
ab_runs/
reports/
